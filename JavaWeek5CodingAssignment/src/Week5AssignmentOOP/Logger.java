package Week5AssignmentOOP;

public interface Logger {
	
	/*
	 * Step 1: created interface above named Logger above
	 * Step 2: added two void methods to the Logger interface below
	 * both take String type as an argument and are labeled as 
	 * Log and Errors as requested 
	 */

	public void Log(String Log);
	public void Error(String Error);
}
